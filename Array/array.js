// La longitud siempre sera una mayoyr al Indice

let colors = ["red", "blue", "yellow", "gray"];
console.log(colors[0])
console.log(colors[1])
console.log(colors[2])

colors[colors.length] = "green";

// Agregamos un nuevo elemento al arreglo
console.log(colors)